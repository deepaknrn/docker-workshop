uv run python multiple_ingest_data.py \
   --pg-user=postgres \
   --pg-password=postgres\
   --pg-host=localhost \
   --pg-port=5433 \
   --pg-db=ny_taxi \
   --target-table1=taxi_zone_data \
   --target-table2=green_trip_data \
   --chunksize=100000

SELECT count(*) FROM green_trip_data where lpep_pickup_datetime 
between '2025-01-11' and '2025-12-01'
and trip_Distance<=1;
count:
8007
